# Гайд по развитию AstraDock

## Как добавить новый корабль

1. Откройте `src/data/ships.ts`
2. Скопируйте структуру одного из существующих кораблей
3. Заполните все поля:
   - `id`: уникальный идентификатор (используйте префиксы как `aer-`, `nyx-` и т.д.)
   - `name`: название модели корабля
   - `class`: один из предустановленных классов
   - `crewMin/crewMax`: минимум и максимум экипажа
   - `availability`: статус доступности
   - `priceUsd`: цена в долларах
   - `badges`: массив значков (LIMITED, PROTOTYPE, CERTIFIED, NEW DROP)
   - `specs`: технические характеристики
   - `features`: список основных фишек
   - `images`: пути к изображениям
   - `trims`: варианты комплектаций
   - `description`: описание для покупателя

## Как добавить новую страницу

1. Создайте компонент в `src/components/PageName.tsx`
2. Добавьте section с id в этом компоненте для навигации
3. Импортируйте компонент в `App.tsx`
4. Добавьте его в main section
5. Обновите навигацию в `Navbar.tsx` если нужна ссылка

## Как изменить цветовую схему

1. Отредактируйте `tailwind.config.js` в секции `theme.extend.colors`
2. Обновите цвета в глобальных компонентах (`Navbar`, `Footer`, и т.д.)
3. Используйте новые классы Tailwind в JSX (например, `text-new-color-name`)

## Как добавить анимации

### Используя Tailwind CSS
```jsx
<div className="transition-all duration-300 hover:scale-105">
  Анимируемый элемент
</div>
```

### Добавить Framer Motion
```bash
npm install framer-motion
```

```jsx
import { motion } from 'framer-motion'

<motion.div
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  transition={{ duration: 0.5 }}
>
  Анимированный контент
</motion.div>
```

## Как добавить новые иконки

Проект использует `lucide-react`. Все доступные иконки можно найти на https://lucide.dev

```jsx
import { IconName } from 'lucide-react'

<IconName size={24} className="text-cyan-holo" />
```

## Как подключить реальный backend

### Для формы контактов

Отредактируйте `App.tsx` в секции Contact:

```jsx
const handleSubmit = async (e) => {
  e.preventDefault()
  
  const formData = {
    name: e.target.name.value,
    email: e.target.email.value,
    // ... другие поля
  }
  
  const response = await fetch('/api/contact', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(formData)
  })
  
  // Обработать ответ
}
```

### Для резервирования корабля

Создайте функцию в `ShipDetail.tsx`:

```jsx
const handleReserve = async (shipId, trimName) => {
  const response = await fetch('/api/reservations', {
    method: 'POST',
    body: JSON.stringify({
      shipId,
      trimName,
      customerName: '...',
      // ...
    })
  })
}
```

## Как добавить поиск кораблей

В `App.tsx` добавьте state для поиска:

```jsx
const [searchTerm, setSearchTerm] = useState('')

const handleSearch = (term) => {
  setSearchTerm(term)
  const filtered = ships.filter(ship =>
    ship.name.toLowerCase().includes(term.toLowerCase()) ||
    ship.description.toLowerCase().includes(term.toLowerCase())
  )
  setDisplayedShips(filtered)
}
```

Затем добавьте input в ShipFilter компонент.

## Как развернуть на Vercel

```bash
# Установите Vercel CLI
npm install -g vercel

# Заходим в папку проекта и развертываем
cd d:\Copilot\spaceship-showroom
vercel
```

Vercel автоматически детектирует Vite проект и создаст оптимальный build.

## Как развернуть на Netlify

```bash
# Build проекта
npm run build

# Загрузите папку dist на Netlify через веб-интерфейс
# или используйте Netlify CLI:
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

## Структура стилей

### Глобальные стили
- Определены в `src/index.css`
- Используют Tailwind @directives
- Содержат @layer для кастомных компонентов

### Компонентные стили
- Используют встроенные классы Tailwind
- Минимум кастомного CSS
- Консистентные цвета и типографика

### Полезные Tailwind классы, определенные в проекте

```
.btn-primary      - основная кнопка
.btn-secondary    - вторичная кнопка
.card-glass       - стеклянная панель
.glow-cyan        - свечение циана
.spec-text        - текст спецификации
.heading-xl       - очень большой заголовок
.heading-lg       - большой заголовок
.heading-md       - средний заголовок
```

## Отладка

### Проверить структуру данных
```bash
# Откройте DevTools (F12) и проверьте консоль
# Убедитесь что нет ошибок при загрузке компонентов
```

### Перестроить CSS
```bash
# Если стили не применяются, переопустите dev сервер
npm run dev
```

### TypeScript ошибки
```bash
# Проверьте типы
npx tsc --noEmit
```

## Best Practices

1. ✅ Используйте компоненты для повторяемых элементов
2. ✅ Сохраняйте данные отдельно от компонентов (в `data/`)
3. ✅ Используйте TypeScript для type safety
4. ✅ Кешируйте computed значения с useMemo если нужно
5. ✅ Оптимизируйте изображения перед добавлением
6. ✅ Тестируйте на мобильных устройствах
7. ✅ Используйте семантичный HTML
8. ✅ Добавляйте alt текст к изображениям
9. ✅ Проверяйте доступность (accessibility)
10. ✅ Документируйте сложные компоненты

## Сортировка и фильтрация

Примеры для улучшения ShipFilter:

```typescript
// Сортировка по цене
const sorted = [...ships].sort((a, b) => a.priceUsd - b.priceUsd)

// Сортировка по имени
const sorted = [...ships].sort((a, b) => a.name.localeCompare(b.name))

// Множественные фильтры
const filtered = ships.filter(ship =>
  ship.priceUsd <= maxPrice &&
  ship.crewMax >= minCrew &&
  ship.class === selectedClass
)
```

## Производительность

- Используйте `React.memo()` для ShipCard если список большой
- Используйте `useCallback()` для функций обработчиков
- Оптимизируйте изображения (используйте WebP если возможно)
- Ленивая загрузка (Lazy Loading) для future расширений

---

**Успехов в развитии AstraDock! 🚀**
